[tool.poetry]
name = "multigrid"
version = "0.1.0"
description = ""
authors = ["Eirik Reiestad <eirik@reiestad.no>"]
readme = "readme.md"
packages = [{ include = "multigrid", from = "." }]

[tool.poetry.dependencies]

python = "^3.11"
numpy = "^2.1.3"
torch = "^2.5.1"
aenum = "^3.1.15"
pygame = "^2.6.1"
pettingzoo = "^1.24.0"
supersuit = "^3.9.3"
ray = { version = "2.7.0", extras = ["tune", "rllib"] }
wandb = "^0.19.0"
pynput = "^1.7.7"

[tool.pyright]
venvPath = "."
venv = ".venv"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
